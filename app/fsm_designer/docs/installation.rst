============
Installation
============

At the command line::

    $ easy_install fsm_designer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fsm_designer
    $ pip install fsm_designer
